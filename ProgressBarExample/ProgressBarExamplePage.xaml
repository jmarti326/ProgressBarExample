<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:ProgressBarExample" x:Class="ProgressBarExample.ProgressBarExamplePage">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <!--Progress Bar -->
        <ProgressBar 
            x:Name="progressbar_increaseByClick" 
            Grid.Column="0" 
            Grid.Row="0"
            WidthRequest="300"
            HeightRequest="300" 
            VerticalOptions="Center" 
            HorizontalOptions="Center"/>

        <!--Increase By Time-->
         <ProgressBar 
            x:Name="progressbar_increaseByTime" 
            Grid.Column="0" 
            Grid.Row="1"
            WidthRequest="300"
            HeightRequest="300" 
            VerticalOptions="Center" 
            HorizontalOptions="Center"/>
        <!-- Increment Button -->
        <Button 
            Clicked="Increase_Clicked" 
            HeightRequest="100" 
            Grid.Row="2" 
            Text="Increase Bar"/>
    </Grid>
</ContentPage>
